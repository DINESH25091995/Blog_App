myenv\Scripts\activate

uvicorn main:app --reload

uvicorn main:app --host 0.0.0.0 --port 10000


To see IPv4
---------------
ipconfig

uvicorn main:app --host 0.0.0.0 --port 8000 --reload




pip install python-multipart itsdangerous

pip install --upgrade bcrypt
pip install --upgrade passlib
pip show bcrypt
pip show passlib
pip install fastapi-sessions
pip install psycopg2-binary


SQL for postgreSQL
------------------------
open psql and login with username and password then run below query

CREATE DATABASE mydatabase;
CREATE USER myuser WITH PASSWORD 'mypassword';
ALTER ROLE myuser SET client_encoding TO 'utf8';
ALTER ROLE myuser SET default_transaction_isolation TO 'read committed';
ALTER ROLE myuser SET timezone TO 'UTC';
GRANT ALL PRIVILEGES ON DATABASE mydatabase TO myuser;


open pgadmin then will be able to see the database

then give the superuser access to create schema
ALTER ROLE myuser WITH SUPERUSER;





